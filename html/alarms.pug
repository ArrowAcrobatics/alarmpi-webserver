include alarm-mixin

doctype html
html(lang="en")
    head
        include alarms-head
    body
        script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/js/bootstrap.bundle.min.js", integrity="sha384-HwwvtgBNo3bZJJLYd8oVXjrBZt8cqVSpeBNS5n7C8IVInixGAoxmnlMuBnhbgrkm", crossorigin="anonymous")
        link(href="https://cdn.jsdelivr.net/npm/bootstrap5-toggle@5.0.4/css/bootstrap5-toggle.min.css", rel="stylesheet")
        script(src="https://cdn.jsdelivr.net/npm/bootstrap5-toggle@5.0.4/js/bootstrap5-toggle.ecmas.min.js")

        script(src="js/main.js", type="module")
        .d-flex.justify-content-center
            .thecontent
                h1 Alarmpi 0.2
        #alarm-container.d-flex.justify-content-center
            ul#sortable(style="width: 25em")
                each alarm in alarms
                    +alarm_mixin(alarm)
        .d-flex.justify-content-center
            .d-flex.justify-content-end(style="width: 25em")
                button.btn.btn-primary.btn-info.alarmpi-add
                    i.fas.fa-plus(aria-hidden="true")
        .d-flex.justify-content-center
            .thecontent
                h3 TODO
                ul
                    li git
                    li python daemon
                    li launcher script
                    li local/wifi network
                    li port to rpi
                    li gpio buttons
                    li music selection
                    li mobile ready
                    li refresh button front/backend
                    li prettyfi newly added button by bootstrapToggle() call
                    li file write throttle for sd card burn in reduction

        include alarms-head
